{
  "name": "re-formality",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "yarn run res:watch",
    "build": "rescript build -with-deps",
    "watch": "rescript build -w",
    "clean": "rescript clean",
    "format": "rescript format -all"
  },
  "dependencies": {
    "rescript-debounce": "1.0.1"
  },
  "devDependencies": {
    "@rescript/react": "0.11.0",
    "rescript": "10.1.3"
  }
}
